pre-commit:
  jobs:
    - name: oxlint
      glob: "*.{js,ts,jsx,tsx,mjs,cjs}"
      run: pnpm run lint:github
    - name: oxfmt
      glob: "*.{js,ts,jsx,tsx,mjs,cjs}"
      run: pnpm exec oxfmt --write {staged_files}
      stage_fixed: true
